#include "Button.h"

using namespace DavinciEngine;

Button::Button()
{
}

Button::~Button()
{
}

void Button::Update()
{
	return;
}

void Button::InputChecks()
{
	// If the UI Control has no inputs then we don't want to waste our time here.
	if (!m_mapInputs.empty() || m_pInput == nullptr) {
		// What do we do when this UI Control is clicked with the left mouse button?
		if (m_pInput->IsMouseButtonPressed(MOUSE_BUTTON_LEFT)) {

		}
		else if (m_pInput->IsMouseButtonPressed(MOUSE_BUTTON_RIGHT)) {

		}
	}
}